services:
  - type: web
    name: server
    runtime: node
    plan: free
    buildCommand: |
      export NODE_VERSION=20.0.0
      npm install -g pnpm
      pnpm install
      pnpm run build
    startCommand: pnpm start
    envVars:
      - key: NODE_VERSION
        value: 20.0.0